name: Build Angular App

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory:  /home/runner/work/medication_taper/medication_taper/medication_taper  # 👈 change to your folder name
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Write environment file
        run: |
          printf 'export const environment = {\n  production: true,\n  apiUrl: "%s"\n};\n' "$API_URL" > ./src/environments/environment.prod.ts
        env:
          API_URL: ${{ secrets.API_URL }}
        
      - name: Set up Node.js
        uses: actions/setup-node@v6        
        with:
          node-version: '22'   # use 22 or 24, avoid 20
          cache: 'npm'
          cache-dependency-path: /home/runner/work/medication_taper/medication_taper/medication_taper
        
      - name: Install dependencies
        run: npm ci
        working-directory:  /home/runner/work/medication_taper/medication_taper/medication_taper
        
      - name: Build
        run: npm run build -- --configuration production

      - name: List directory contents
        run: ls -la /home/runner/work/medication_taper/medication_taper/medication_taper/dist
        
      - name: Deploy via rsync
        uses: easingthemes/ssh-deploy@main
        with:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          ARGS: "-avz --no-times --delete"
          SOURCE: "medication_taper/dist/medication_taper/browser/"
          REMOTE_HOST: ${{ secrets.REMOTE_HOST }}
          REMOTE_USER: ${{ secrets.REMOTE_USER }}
          TARGET: /home/brian/artifact
